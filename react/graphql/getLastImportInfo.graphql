query getLastImportInfo {
  lastImportedCommissionFileInfo
    @context(provider: "agencianairuzpartnerbr.affiliates-commission-service") {
    fileId
    filename
    uploadDate
    uploadedBy
  }
}
